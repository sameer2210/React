import React from "react";

const a = 5;
const b = 10;
const add = a + b;
const Sum = () => {
  return (
    <div>
      <h1>sum of a = {a} +  and b = {b} is {add}</h1>
    </div>
  );
};

export default Sum;
